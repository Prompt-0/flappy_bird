# Test-Driven Development (TDD)
Write test first, watch fail (RED), minimal fix (GREEN), clean up (REFACTOR).
- Never add production code without a failing test first.
- Make tests explicit, testing behavior rather than implementation details.
